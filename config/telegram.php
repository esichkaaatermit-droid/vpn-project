<?php

/**
 * Конфигурация Telegram бота.
 * 
 * Все параметры берутся из .env файла.
 */

return [
    /*
    |--------------------------------------------------------------------------
    | Bot Token
    |--------------------------------------------------------------------------
    |
    | Токен бота, полученный от @BotFather в Telegram.
    | Пример: 123456789:ABCDefGhIJKlmnOPQrstUVwxyz
    |
    */
    'bot_token' => env('TELEGRAM_BOT_TOKEN'),

    /*
    |--------------------------------------------------------------------------
    | Webhook URL
    |--------------------------------------------------------------------------
    |
    | URL для получения обновлений от Telegram.
    | Должен быть HTTPS и доступен из интернета.
    | Пример: https://yourdomain.com/api/telegram/webhook
    |
    */
    'webhook_url' => env('TELEGRAM_WEBHOOK_URL'),

    /*
    |--------------------------------------------------------------------------
    | Webhook Secret Token
    |--------------------------------------------------------------------------
    |
    | Секретный токен для верификации запросов от Telegram.
    | Telegram отправляет его в заголовке X-Telegram-Bot-Api-Secret-Token.
    |
    */
    'webhook_secret' => env('TELEGRAM_WEBHOOK_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Bot Settings
    |--------------------------------------------------------------------------
    |
    | Дополнительные настройки бота.
    |
    */
    'settings' => [
        // Экран по умолчанию при команде /start
        'start_screen' => env('TELEGRAM_START_SCREEN', 'main.menu'),
        
        // Время жизни состояния пользователя (в часах)
        'state_ttl_hours' => env('TELEGRAM_STATE_TTL', 24),
    ],
];
