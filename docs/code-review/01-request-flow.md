# Путь запроса и выбор обработчика

> Как Telegram-запрос проходит через систему и попадает в нужный хендлер

---

## Общая цепочка за 30 секунд

```
Telegram отправляет POST-запрос
        ↓
routes/api.php              — маршрут /api/telegram/webhook
        ↓
WebhookController::handle() — валидация токена, передача в сервис
        ↓
BotService::handleUpdate()  — определяет тип: message или callback_query
        ↓
    ├── handleMessage()         — текстовые сообщения и команды
    └── handleCallbackQuery()   — нажатия inline-кнопок
                ↓
BotService::showScreen()    — загружает экран из БД по ключу
        ↓
HandlerRegistry::execute()  — если у экрана есть handler_id, вызывает хендлер
        ↓
ConcreteHandler::handle()   — формирует динамический контент
        ↓
BotService::sendMessage()   — отправляет ответ в Telegram
```

---

## Шаг 1. Маршрутизация (Laravel)

Файл: `routes/api.php`

```php
Route::prefix('telegram')->group(function () {
    Route::post('/webhook', [WebhookController::class, 'handle']);
});
```

Все запросы от Telegram идут на один endpoint. Никакой дополнительной маршрутизации на уровне Laravel нет — дальнейший «роутинг» происходит внутри `BotService`.

---

## Шаг 2. Контроллер — точка входа

Файл: `app/Http/Controllers/Telegram/WebhookController.php`

Контроллер делает три вещи:

1. **Проверяет секретный токен** — Telegram передаёт его в заголовке `X-Telegram-Bot-Api-Secret-Token`
2. **Логирует** входящий update в debug-режиме
3. **Передаёт** update в `BotService::handleUpdate()`

Важный момент — контроллер всегда возвращает HTTP 200, даже при ошибке. Это сделано намеренно: если вернуть ошибку, Telegram будет повторять запрос.

```php
// WebhookController::handle(), строка 30–45
try {
    $this->botService->handleUpdate($update);
    return response()->json(['status' => 'ok']);
} catch (\Throwable $e) {
    // Возвращаем 200, чтобы Telegram не ретраил
    return response()->json(['status' => 'error', 'message' => $e->getMessage()]);
}
```

---

## Шаг 3. BotService — диспетчер запросов

Файл: `app/Services/Telegram/BotService.php`

### Определение типа запроса (строки 37–44)

```php
public function handleUpdate(array $update): void
{
    if (isset($update['message'])) {
        $this->handleMessage($update['message']);
    } elseif (isset($update['callback_query'])) {
        $this->handleCallbackQuery($update['callback_query']);
    }
}
```

Два типа входящих событий:
- **message** — пользователь написал текст или отправил команду (`/start`)
- **callback_query** — пользователь нажал inline-кнопку

### Обработка текстового сообщения (строки 49–85)

Логика выбора экрана при текстовом сообщении:

```
1. Команда /start?
   → Да: показать стартовый экран (main.menu)

2. У пользователя есть текущий экран?
   → Да: ищем кнопку с таким текстом в текущем экране
       → Нашли: переходим на next_screen_key этой кнопки

3. Ничего не подошло?
   → Показываем главное меню (fallback)
```

Ключевой момент — **текст сообщения сопоставляется с текстом кнопок** текущего экрана. Это позволяет обрабатывать reply-клавиатуру без жёсткого кодирования команд.

### Обработка callback-запроса (строки 90–106)

При нажатии inline-кнопки всё проще:

```php
$data = $callbackQuery['data'] ?? '';  // data = screen_key
$this->showScreen($chatId, $data, $userState, $messageId);
```

`callback_data` inline-кнопки — это ключ следующего экрана. Прямое соответствие: нажал кнопку → показали экран.

---

## Шаг 4. Показ экрана и вызов хендлера (строки 116–184)

Это **центральный метод** всей системы — `showScreen()`:

```php
public function showScreen(int $chatId, string $screenKey, ...): bool
{
    // 1. Загружаем экран из БД по ключу
    $screen = Screen::findByKey($screenKey);

    // 2. Обновляем состояние пользователя
    $userState->setCurrentScreen($screenKey);

    // 3. Берём текст и кнопки по умолчанию из экрана
    $text = $screen->text;

    // 4. Если у экрана есть handler_id — вызываем хендлер
    if ($screen->hasHandler()) {
        $result = HandlerRegistry::execute($screen->handler_id, $screen, $chatId, [
            'user' => $user,
            'user_state' => $userState,
        ]);
        // Хендлер может переопределить текст, кнопки, добавить фото/файл
    }

    // 5. Если хендлер не вернул кнопки — строим из БД
    if (empty($buttons)) {
        $buttons = $this->buildButtonsFromScreen($screen);
    }

    // 6. Отправляем/редактируем сообщение
}
```

Принцип: **экран = базовый контент из БД + опциональное обогащение через хендлер**.

---

## Шаг 5. Реестр хендлеров (HandlerRegistry)

Файл: `app/Bot/HandlerRegistry.php`

Реестр — это статический маппинг строковых ID на классы:

```php
protected static array $handlers = [
    'main.menu'          => MainMenuHandler::class,
    'profile.my_profile' => ProfileHandler::class,
    'tariffs.pricing'    => TariffHandler::class,
];
```

**Как происходит выбор хендлера:**

1. В БД у экрана есть поле `handler_id` (например, `'profile.my_profile'`)
2. `HandlerRegistry::execute()` ищет этот ID в массиве `$handlers`
3. Если нашёл — создаёт экземпляр класса через `app()` (Laravel DI-контейнер)
4. Вызывает `handle($screen, $chatId, $update)`
5. Если не нашёл — логирует warning, экран показывается с данными из БД

Хендлеры кешируются как singleton — один экземпляр на запрос.

---

## Шаг 6. Конкретные хендлеры

Файл: `app/Bot/Handlers/`

Все хендлеры реализуют `HandlerInterface`:

```php
interface HandlerInterface
{
    public function handle(Screen $screen, int $chatId, array $update): array;
}
```

Возвращают массив с ключами: `text`, `buttons`, `photo`, `document`.

| Хендлер | Что делает | Внешние зависимости |
|---------|-----------|---------------------|
| `MainMenuHandler` | Просто возвращает текст и кнопки экрана | Нет |
| `ProfileHandler` | Формирует текст профиля с данными подписки | `UserService` |
| `TariffHandler` | Подгружает актуальные тарифы | `TariffService` |

---

## Визуально: что происходит при нажатии кнопки «Мой профиль»

```
Пользователь нажимает inline-кнопку "Мой профиль"
        ↓
callback_data = "profile.my_profile"
        ↓
WebhookController → BotService::handleCallbackQuery()
        ↓
BotService::showScreen("profile.my_profile")
        ↓
Screen::findByKey("profile.my_profile")  →  экран из БД, handler_id = "profile.my_profile"
        ↓
screen->hasHandler() = true
        ↓
HandlerRegistry::execute("profile.my_profile", ...)
        ↓
ProfileHandler::handle()  ←  получает UserService через DI
        ↓
UserService::getUserProfile()  →  данные о подписке
        ↓
Возвращает ['text' => "👤 Мой профиль...", 'buttons' => [...]]
        ↓
BotService::editMessage()  →  обновляет сообщение в Telegram
```

---

## Где что искать (шпаргалка)

| Вопрос | Файл | Строки |
|--------|------|--------|
| Маршрут webhook | `routes/api.php` | 7 |
| Валидация токена | `WebhookController.php` | 24–30 |
| Определение типа update | `BotService.php` | 37–44 |
| Логика обработки текста | `BotService.php` | 49–85 |
| Обработка callback-кнопок | `BotService.php` | 90–106 |
| Показ экрана + вызов хендлера | `BotService.php` | 116–184 |
| Маппинг handler_id → класс | `HandlerRegistry.php` | 27–31 |
| Выполнение хендлера | `HandlerRegistry.php` | 65–83 |
| Интерфейс хендлера | `HandlerInterface.php` | 19 |
